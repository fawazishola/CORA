# CORA Structure Notes

## Naming

The project uses `CORA` as its name:

- `CORA` = Core Operating & Reasoning Appliance

## Structure

The repo is organized into:

- `docs/`
- `hardware/`
- `renders/`
- `sim/`
